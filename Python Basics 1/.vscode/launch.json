{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug active file (external Terminal)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}",
      "console": "externalTerminal",
      "justMyCode": true

      /*  delete or comment this so it won’t run the task first
      ,"preLaunchTask": "Run active Python file (Terminal.app)"
      */
    }
  ]
}
